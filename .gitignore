# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# 本地跑出来的报告（基线本身要入库，报告不必）
reports/
*.local.json

# 本机私有配置：指定本机带 torch 的解释器等（含个人路径，绝不入库）
pasm-skills.local.json

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db

# 打包过程中在本仓根目录解压出的 sdist 工作目录（构建残留，勿入库）
pasm_skills-*/
