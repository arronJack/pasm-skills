> 本文件是 **{{SKILL_NAME}} 技能 SKILL.md 的正文部分**（不含 frontmatter）。
> `tools/build_skill.py` 会把它拼上两种归档形态的 frontmatter（`zip-root` / `slug-dir`），产出可直接上传的包。
>
> 复制本模板并替换所有 `{{...}}` 占位符。正文是**给智能体读的说明书**，
> 不是给人看的宣传页 —— 写清楚"什么时候用、怎么调、有什么坑"。

# {{智能体名字}}（{{skill-name}}）

一句话说清它是什么、基于什么、有什么硬约束（零 LLM 依赖 / 断网可用 / 状态持久化…）。

```python
from {{package}} import {{ClassName}}

a = {{ClassName}}(agent_id="{{demo_id}}", persona={
    # 这里放一段**能直接跑**的最小示例，别放伪代码
})
print(a.act())
print(a.chat("{{一句示例问话}}"))
a.save()
```

## 什么时候用

- {{具体场景 1 —— 写"需要 XX 时用"，不要写"功能强大"}}
- {{具体场景 2}}
- {{具体场景 3}}

## 0. 铁律

1. **{{最重要的约束}}**：{{为什么，以及违反会怎样}}。
2. **档位透明**：优先驱动 PASM 真核心，拿不到就降级，`tier` 字段如实写明，**永不隐藏降级**。
3. **{{安全/正确性红线}}**：{{这条如果坏了会造成什么后果}}。

## 1. 30 秒上手

```bash
pip install {{依赖}}
{{一行命令就能看到效果的命令}}
```

## 2. persona 里有什么

| 字段 | 必填 | 说明 |
|---|---|---|
| `name` | ✅ | {{...}} |
| {{字段}} | {{是/否}} | {{...}} |

## 3. 核心 API

{{逐个方法：签名 + 一段可跑示例 + 返回值长什么样。**示例必须实跑过。**}}

## 4. 交互模式

| 命令 | 作用 |
|---|---|
| `{{cmd}}` | {{...}} |

## 5. 档位与降级

| tier | 含义 | 何时启用 |
|---|---|---|
| `bionic` | 完整 PASM 核心 + emotion | 有 torch |
| `core`   | PASM 核心（memory + learning） | 能 import `pasm.cognitive` |
| `light`  | 纯内置实现 | 任何机器 |

## 6. 已知短板（如实说明）

{{这一节**不能省**。做不到的事写清楚，比假装能做到更可信。
   写"实测数值是多少、为什么做不到、要提升该怎么绕"。}}

## 7. 接入你自己的产品

```python
{{最小可跑的集成片段}}
```

## 8. 相关

- {{同族技能}}
- 基座：`pasm-skills`（SDK / 打包工具 / 开发文档）
